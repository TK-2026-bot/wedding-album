// TODO: replace with the real values from
// Firebase Console → Project settings → General → Your apps → SDK setup and configuration
// Also requires the Blaze (pay-as-you-go) billing plan enabled for Cloud Storage to work.
export const firebaseConfig = {
  apiKey: "REPLACE_ME",
  authDomain: "REPLACE_ME.firebaseapp.com",
  projectId: "REPLACE_ME",
  storageBucket: "REPLACE_ME.appspot.com",
  messagingSenderId: "REPLACE_ME",
  appId: "REPLACE_ME",
};

// Single wedding event this whole app is scoped to.
// Change if you ever need to host more than one event from the same project.
export const EVENT_ID = "mallorca-wedding";
